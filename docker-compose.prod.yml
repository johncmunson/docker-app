version: '3'
services:
  auth:
    environment:
      - NODE_ENV=production
  mail:
    environment:
      - NODE_ENV=production
  # migrate:
  postgres:
    volumes:
      - postgres-data:/var/lib/postgresql/data
  # redis:
  nginx:
    ports:
      - "80:80"
volumes:
  postgres-data:
