# common config, available to all services, goes here

DEV_PROTOCOL=http
PROD_PROTOCOL=http

DEV_HOST=localhost
PROD_HOST=142.93.76.92

DEV_PORT=3000
PROD_PORT=80

# MAIL_PORT not yet needed b/c it just connects to redis at the moment,
# and not the outside world
FRONTEND_PORT=3000
AUTH_PORT=3000
REDIS_PORT=6379
POSTGRES_PORT=5432

FRONTEND_VIRTUAL_HOST_DEV=localhost
FRONTEND_VIRTUAL_HOST_PROD=change-me-when-deployed

AUTH_VIRTUAL_HOST=auth
MAIL_VIRTUAL_HOST=mail
MIGRATE_VIRTUAL_HOST=migrate
POSTGRES_VIRTUAL_HOST=postgres
REDIS_VIRTUAL_HOST=redis
